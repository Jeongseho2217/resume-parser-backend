spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://svc.sel3.cloudtype.app:30522/capstonedb
    username: root
    password: 12341234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true	